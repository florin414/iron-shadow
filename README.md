# iron-shadow
A high-throughput Digital Twin platform written in Rust. Implements the "Device Shadow" pattern to synchronize state between edge devices (MQTT) and the cloud, capable of handling 1M+ concurrent connections with minimal memory footprint.
